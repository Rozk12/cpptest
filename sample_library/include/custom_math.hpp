#ifndef CUSTOM_MATH_HPP
#define CUSTOM_MATH_HPP

namespace test_library {

    // Function to sum two numbers
    // template <typename T>
    // T sum(T a, T b);
    double sum(double a, double b);
}

#endif // CUSTOM_MATH_HPP